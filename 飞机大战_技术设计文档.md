# 🚀 星际雷霆 · 飞机大战 技术设计文档

> **版本**: v1.0  
> **文档类型**: 技术规格说明书（供 Solo Builder 实现使用）  
> **目标平台**: Web 浏览器（桌面 + 移动端适配）  
> **风格定位**: 赛博朋克 · 未来科技风

---

## 一、项目概述

### 1.1 游戏简介
《星际雷霆》是一款竖版飞行射击（STG）网页小游戏，融合了《雷霆战机》《逆空使命》《全民雷电》等爆火产品的核心玩法特色，主打：
- **快节奏弹幕射击**：单手即可操作，自动射击，滑动走位
- **Roguelike 技能组合**：每局随机技能强化，千种构筑流派
- **多阶段 Boss 战**：形态切换、弹幕变化、阶段狂暴
- **多种游戏模式**：闯关模式、无尽模式、限时挑战
- **赛博朋克视觉**：霓虹光效、粒子爆炸、动态星空背景

### 1.2 设计参考（市面爆火玩法融合）
| 参考游戏 | 融合元素 |
|---------|---------|
| 雷霆战机：集结 | 战机 DIY 系统、战神机甲暴走模式 |
| 战机代号666 | Roguelike 随机技能、炮塔舰队僚机系统 |
| 逆空使命 | 三色芯片技能组合、四天王合体 Boss |
| 全民雷电 | 多阶机体进化、量子护盾激光矩阵 |
| 黑鹰战机 | 连击系统 + 战斗特技系统 |

---

## 二、技术栈选型

### 2.1 核心技术（纯前端，无后端依赖）
| 层面 | 技术选择 | 选型理由 |
|-----|---------|---------|
| 语言 | **HTML5 + CSS3 + JavaScript (ES6+)** | 原生实现，零依赖，Solo Builder 最易理解和调试 |
| 渲染引擎 | **Canvas 2D API** | 性能足够，绘制粒子/弹幕灵活，无需额外学习成本 |
| 音频 | **Web Audio API（程序化合成）** | 零音频文件依赖，动态合成 BGM 与 SFX |
| 数据持久化 | **localStorage** | 存档最高分数、战机解锁进度、设置选项 |
| 动画 | **requestAnimationFrame + Tween 缓动** | 60FPS 流畅动画，屏幕震动、缓动特效 |

### 2.2 为什么不选框架？
考虑到 Solo Builder 的理解成本和快速实现需求，**不引入 React/Vue/Pixi.js 等框架**，全部使用原生 Canvas + JS 实现。代码结构分层清晰，单文件可运行（也可拆分多文件）。

---

## 三、视觉风格系统（科技风设计规范）

### 3.1 核心配色方案（赛博朋克霓虹）
```css
/* 主色调 - 深空蓝 */
--bg-deep:       #0a0e27;   /* 背景底色 */
--bg-space:      #0d1b3e;   /* 深空渐变 */
--bg-panel:      rgba(16, 28, 68, 0.85); /* UI面板半透明 */

/* 主色光 - 霓虹青 */
--neon-cyan:     #00f0ff;   /* 主UI光、玩家子弹 */
--neon-cyan-dim: #00a8b3;   /* 暗光版本 */

/* 辅色光 - 能量紫 */
--neon-purple:   #b400ff;   /* 道具、技能特效 */
--neon-pink:     #ff00aa;   /* 危险警告、Boss 血条 */

/* 功能色 */
--neon-green:    #39ff14;   /* 恢复、护盾、增益 */
--neon-yellow:   #fff200;   /* 金币、经验、警告 */
--neon-orange:   #ff6b00;   /* 敌机子弹、爆炸 */
--neon-red:      #ff003c;   /* 致命危险、低血量 */

/* 文字色 */
--text-primary:  #e6f1ff;   /* 主文字 */
--text-secondary:#8aa0c4;   /* 次文字 */
--border-glow:   rgba(0, 240, 255, 0.4); /* 边框发光 */
```

### 3.2 视觉效果清单
| 效果类型 | 实现方式 | 应用场景 |
|---------|---------|---------|
| **动态星空背景** | 3层视差滚动星星 + 随机流星 | 游戏全过程背景 |
| **霓虹发光文字** | Canvas shadowBlur + 双层描边 | 标题、分数、UI文字 |
| **扫描线网格** | 半透明横线 + 竖线渐变 | 开始界面、暂停界面背景 |
| **粒子爆炸** | 圆形粒子 + 径向渐变 + alpha衰减 | 敌机击毁、Boss爆炸 |
| **屏幕震动** | canvas 随机偏移 2-4px | 中弹、Boss爆炸、技能释放 |
| **HUD 面板** | 渐变矩形 + 发光边框 + 圆角 | 血量条、能量条、分数框 |
| **拖尾效果** | 历史位置数组 + 透明度渐变 | 玩家战机引擎尾焰、导弹 |
| **激光束** | 渐变矩形 + 外发光 + 脉动动画 | 激光武器、Boss 激光 |
| **脉冲光环** | expand + alpha衰减圆环 | 护盾激活、技能释放 |

---

## 四、核心玩法系统设计

### 4.1 操作方式（双端适配）

| 操作 | 桌面端（键鼠） | 移动端（触屏） |
|-----|--------------|--------------|
| **移动战机** | 鼠标拖拽 / WASD / 方向键 | 单指滑动拖拽（战机跟随手指） |
| **释放必杀技（炸弹）** | 空格键 / 鼠标右键 | 双指点击 / 右下角按钮 |
| **暂停游戏** | P 键 / ESC 键 | 左上角暂停按钮 |
| **自动射击** | ✅ 默认开启，无需操作 | ✅ 默认开启，无需操作 |

> 💡 **核心设计理念**：自动射击是爆火飞机大战的标配！让玩家专注走位和策略抉择。

### 4.2 游戏模式（3+1 种模式）

#### 🎯 模式一：闯关模式（PVE 主线）
- **关卡数量**：6 章 × 5 小关 = 30 关
- **关卡节奏**：每关 2-3 分钟，第 5 小关为 Boss 关
- **难度递进**：敌机数量↑、弹幕密度↑、敌机血量↑
- **章节主题**：
  1. 第1章：地球近地轨道（入门）
  2. 第2章：月球基地攻防
  3. 第3章：小行星带突围
  4. 第4章：火星殖民地保卫
  5. 第5章：暗星云穿越
  6. 第6章：敌方母舰决战（最终章）

#### ♾️ 模式二：无尽模式（冲击排行榜）
- **核心机制**：敌机无限刷新，难度随时间持续提升
- **波次系统**：每 5 波触发一个小 Boss，每 15 波触发大 Boss
- **生存奖励**：每存活 1 分钟获得额外金币奖励
- **专属道具**：无尽模式掉落稀有强化芯片

#### ⏱️ 模式三：限时挑战（3 分钟火力全开）
- **规则**：3 分钟倒计时，敌机高密度刷新
- **倍率机制**：连击不断，分数倍率从 ×1 最高叠加到 ×10
- **评价等级**：S/A/B/C/D 五档评级，S 级解锁限定战机涂装

#### 🎲 模式四：Roguelike 远征（隐藏模式）
- **解锁条件**：通关第 3 章后解锁
- **核心机制**：
  - 开局 3 条命，10 个随机节点
  - 每个节点 3 选 1 技能强化（永久叠加到本局）
  - 节点类型：战斗节点 / Boss 节点 / 商店节点 / 休息节点
  - 失败需从头开始，但累计的远征积分可兑换永久强化

### 4.3 战机系统（4 架基础战机 + 2 架隐藏解锁）

| 战机代号 | 名称 | 属性定位 | 武器特点 | 解锁条件 |
|---------|------|---------|---------|---------|
| F-01 | **雷霆先锋** | 均衡型 | 标准双发散射，性能均衡无短板 | 默认拥有 |
| F-02 | **暗影刺客** | 高速型 | 单发高伤子弹，移速快，机身瘦小判定小 | 通关第2章 |
| F-03 | **重装堡垒** | 重火力型 | 四发散射，高血量，移动慢 | 通关第4章 |
| F-04 | **量子幽灵** | 技巧型 | 三发追踪弹，15%概率闪避伤害 | 无尽模式存活10分钟 |
| F-05 | **灭世神罚** | 狂暴型 | 六发散射+激光副武器，全能输出 | S级通关所有关卡 |
| F-06 | **虚空主宰** | 终极型 | 全屏弹幕+黑洞技能 | 远征模式通关1次 |

#### 战机属性参数（数值模板）
```javascript
const PLANE_TEMPLATES = {
  F01: {
    name: "雷霆先锋",
    maxHp: 100,          // 生命值
    speed: 5,            // 移动速度
    fireRate: 150,       // 射击间隔(ms)
    bulletDamage: 10,    // 基础伤害
    bulletSpeed: 12,     // 子弹速度
    bulletType: "double_scatter", // 弹道类型
    hitboxSize: 20,      // 碰撞判定半径（越小越灵活）
    skill: "thunder_strike", // 必杀技ID
    skillCooldown: 20000 // 技能CD(ms)
  }
  // ... 其他战机类似结构
}
```

### 4.4 武器进化系统（火力升级 Lv1 → Lv8）

玩家吃到 **P 道具（PowerUp）** 可提升火力等级，每级弹道变化：

| 火力等级 | 弹道形态（以F-01为例） | 视觉表现 |
|---------|----------------------|---------|
| Lv1 | 单发直线子弹 | 基础青色光弹 |
| Lv2 | 双发平行子弹 | 左右各一发 |
| Lv3 | 三发扇形（5°夹角） | 扇形散射 |
| Lv4 | 三发扇形 + 导弹×1 | 额外追踪导弹 |
| Lv5 | 五发扇形（8°夹角） | 宽广弹幕 |
| Lv6 | 五发扇形 + 导弹×2 + 僚机×1 | 左侧无人机僚机 |
| Lv7 | 七发扇形 + 导弹×3 + 僚机×2 | 双侧僚机 |
| Lv8（MAX）| 七发扇形 + 导弹×4 + 僚机×2 + 激光束 | 中央高能激光贯穿 |

> 💡 **受击降级机制**：玩家中弹时，火力等级 -1（最低 Lv1），形成「中弹→火力变弱→更容易死」的紧张感，符合经典 STG 设计。

### 4.5 Roguelike 技能芯片系统（每局随机构筑）

每局游戏中击杀敌机有概率掉落 **技能芯片**（或升级时 3 选 1），芯片分为 3 类：

#### 🔴 红色芯片（攻击强化）
| 芯片名 | 效果 |
|-------|------|
| 火力核心 | 子弹伤害 +20%（可叠加，最多×3） |
| 极速射击 | 射速提升 25%（最多×3） |
| 穿透弹头 | 子弹可穿透 1 个敌人 |
| 爆裂弹头 | 子弹命中后小范围爆炸 |
| 弹片风暴 | 每次射击额外射出 2 发斜向弹 |
| 弱点锁定 | 对 Boss 伤害额外 +30% |

#### 🔵 蓝色芯片（防御生存）
| 芯片名 | 效果 |
|-------|------|
| 量子护盾 | 开局自带 1 层护盾（吸收 1 次伤害） |
| 纳米修复 | 每 30 秒恢复 10% 血量 |
| 相位闪避 | 10% 概率闪避子弹（每级 +5%） |
| 装甲强化 | 最大生命值 +30 |
| 伤害缓冲 | 受到伤害减少 20% |
| 复活协议 | 死亡后复活 1 次（血量 50%） |

#### 🟡 黄色芯片（特殊功能）
| 芯片名 | 效果 |
|-------|------|
| 磁铁引力 | 道具自动吸附范围 +100% |
| 金币翻倍 | 所有金币获取 ×2 |
| 经验汲取 | 经验获取速度 +50% |
| 冷却缩减 | 必杀技冷却时间 -30% |
| 僚机指挥 | 额外增加 1 架僚机（最多+2） |
| 时间扭曲 | 全屏敌人速度降低 15% |

### 4.6 道具系统（8 种掉落道具）

击毁敌机后随机掉落，带磁吸范围（玩家靠近自动吸附）：

| 道具图标 | 名称 | 效果 | 掉落概率 |
|---------|------|------|---------|
| 🔷 P | 火力强化 | 火力等级 +1（上限Lv8） | 12% |
| 💠 S | 护盾 | 获得 1 层量子护盾（抵挡1次伤害） | 6% |
| ❤️ H | 生命恢复 | 恢复 25% 最大生命值 | 5% |
| 💰 C | 金币 | 获得金币（10-50 随机） | 20% |
| ⚡ E | 能量 | 必杀技能量 +30% | 8% |
| 🛡️ B | 炸弹 | 获得 1 枚全屏清屏炸弹 | 3% |
| 🧩 X | 技能芯片 | 随机获得 1 个技能芯片（3选1） | 4% |
| ⭐ EXP | 经验球 | 获得经验值（升级提升基础属性） | 15% |
| —— 空 —— | —— | 什么都不掉 | 27% |

---

## 五、敌机与 Boss 系统设计

### 5.1 普通敌机类型（5 种基础敌机）

| 类型 | 名称 | 外观描述 | 行为模式 | 血量 | 分值 |
|-----|------|---------|---------|------|------|
| 🟠 小型 | **侦察机** | 橙色流线型，体积小 | 直线匀速下降，不射击 | 20 | 100 |
| 🔴 中型 | **战斗机** | 红色三角翼，带机枪 | 直线下降，每隔2秒射击1发 | 60 | 300 |
| 🟣 大型 | **轰炸机** | 紫色重型，体积大 | 缓慢下降，每3秒扇形射3发 | 150 | 800 |
| 🔵 高速 | **截击机** | 蓝色尖锐形 | 快速俯冲，S形曲线走位 | 40 | 500 |
| 🟡 召唤型 | **母舰** | 金色圆形平台 | 悬停在屏幕上方，每5秒召唤2架小飞机 | 200 | 1200 |

### 5.2 Boss 系统设计（3 级 Boss × 多阶段）

#### Boss 分级规则
- **小 Boss（Sub-Boss）**：每章第 2、4 关出现，血量约为该关普通敌机的 20 倍
- **章节 Boss（Chapter Boss）**：每章第 5 关出现，3 阶段攻击模式切换
- **终极 Boss（Final Boss）**：第 6 章 Boss，4 阶段 + 合体形态

#### Boss 通用数据结构
```javascript
const BOSS_TEMPLATE = {
  id: "BOSS_CH1",
  name: "轨道防卫者·MK-I",
  maxHp: 3000,
  phases: [
    // 阶段1：血量 100% → 66%
    {
      hpThreshold: 0.66,
      attackPatterns: ["spread_5", "aimed_shot"],
      movePattern: "horizontal_pan", // 水平左右移动
      attackInterval: 1500
    },
    // 阶段2：血量 66% → 33%
    {
      hpThreshold: 0.33,
      attackPatterns: ["spiral_shot", "spread_8", "summon_minions"],
      movePattern: "figure_8", // 8字移动
      attackInterval: 1000,
      onEnter: "roar_sfx + screen_shake" // 阶段切换特效
    },
    // 阶段3：血量 33% → 0%（狂暴阶段）
    {
      hpThreshold: 0,
      attackPatterns: ["rain_bullets", "laser_beam", "cross_burst"],
      movePattern: "chase_player", // 追击玩家位置
      attackInterval: 600,
      berserkMode: true, // 狂暴标记：颜色变红，背景闪烁
      onEnter: "berserk_transformation"
    }
  ]
}
```

#### 12 种 Boss 弹幕模式（积木式组合）

| 弹幕ID | 名称 | 效果描述 | 视觉效果 |
|--------|------|---------|---------|
| `spread_5` | 扇形散射 5 发 | 朝玩家方向 ±30° 射出 5 发 | 橙色球形弹 |
| `spread_8` | 广域扇形 8 发 | ±60° 射出 8 发密集弹幕 | 紫色弹 |
| `spiral_shot` | 螺旋弹幕 | 每 100ms 旋转 15° 射 1 发，形成螺旋 | 彩虹渐变弹 |
| `aimed_shot` | 瞄准射击 | 计算玩家位置，精准发射追踪弹 | 红色追踪弹 |
| `ring_burst` | 环形爆发 | 360° 同时射出 24 发 | 白色光环弹 |
| `rain_bullets` | 弹幕雨 | 从屏幕上方随机位置落下大量子弹 | 蓝色雨点弹 |
| `laser_beam` | 激光束 | 预热 1 秒 → 发射 2 秒垂直激光 | 红色巨型激光带 |
| `cross_burst` | 十字爆裂 | 射出十字形 4 方向弹幕 | 黄色十字弹 |
| `summon_minions` | 召唤小兵 | 从 Boss 两侧释放小型敌机 | 传送门特效 |
| `windmill` | 风车甩弹 | 4 臂旋转，每臂 3 发子弹 | 风车状旋转 |
| `tracking_missile` | 追踪导弹 | 发射 3 枚导弹追踪玩家 | 烟雾尾焰导弹 |
| `bubble_trap` | 气泡陷阱 | 发射缓慢移动的大体积气泡，阻挡走位 | 半透明紫色气泡 |

#### 6 个章节 Boss 设计

| 章节 | Boss 名称 | 形象描述 | 弹幕特色 | 阶段3狂暴形态 |
|-----|----------|---------|---------|--------------|
| 1 | **轨道防卫者 MK-I** | 圆形飞碟，四周炮管 | 基础扇形 + 瞄准弹 | 双层环形爆发 |
| 2 | **月球炮台·泰坦** | 六边形重型炮台，带护盾 | 激光束 + 召唤小兵 | 护盾碎裂，激光×2 |
| 3 | **小行星·巨蟹** | 岩石拼接的螃蟹形机甲 | 螺旋弹幕 + 气泡陷阱 | 双钳夹击 + 弹幕雨 |
| 4 | **火星巡洋舰·赤龙** | 红龙造型巡洋舰 | 追踪导弹 + 风车甩弹 | 喷吐火焰弹幕 |
| 5 | **暗星云·吞噬者** | 黑洞造型，四周发光环 | 引力场减速 + 随机传送 | 引力反转 + 全屏弹幕 |
| 6 | **母舰核心·神谕**（终BOSS） | 多面体巨型母舰，带核心 | 所有弹幕模式综合 | 四天王合体·终极形态 |

> 💡 **终 Boss 彩蛋**：神谕被击败后，会触发「核心自爆」演出动画，屏幕全白 1 秒，然后玩家战机从爆炸中飞出，获得 S 评价通关特效。

---

## 六、UI / HUD 界面设计

### 6.1 界面流转图
```
┌─────────────┐    ┌──────────────┐    ┌──────────────┐
│  标题界面    │───▶│  模式选择    │───▶│  战机选择    │
└──────┬──────┘    └──────┬───────┘    └──────┬───────┘
       │                  │                   │
       ▼                  ▼                   ▼
┌─────────────┐    ┌──────────────┐    ┌──────────────┐
│   设置      │    │  战报/存档   │    │ Roguelike   │
│  音量/难度  │    │  最高分/成就  │    │  远征商店   │
└─────────────┘    └──────────────┘    └──────────────┘
                           │
                           ▼
                   ┌──────────────┐
                   │   战斗场景   │◀──┐
                   │  (Canvas)    │   │暂停
                   └──────┬───────┘   │
                          │           │
              ┌───────────┼───────────┤
              ▼           ▼           ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │  胜利结算 │ │  失败结算 │ │ 暂停菜单 │
        │ 分数奖励  │ │ 再来一局 │ │ 继续/退出│
        └──────────┘ └──────────┘ └──────────┘
```

### 6.2 战斗内 HUD 布局（竖屏）
```
┌─────────────────────────────────────────┐
│ [⏸暂停]  第1章-第3关          分数 99999│ ← 顶部栏
│ [❤️ HP] ████████████░░░ 72%             │
│ [⚡SP] ███████░░░░░░░░░ 45%    连击 ×5  │ ← 双资源条
│ [🛡️×2] [💣×3] [🔥火力Lv.7]  [⏱ 02:15]  │ ← 状态图标行
│                                         │
│        ★ 敌机 ★      弹幕区域           │
│     (敌机从上方出现)                     │
│                                         │
│                   ✈️                    │ ← 玩家战机
│               (底部中央)                │
│                                         │
│                          [💥 必杀按钮]   │ ← 右下角
└─────────────────────────────────────────┘
     ↑ 左侧吸附道具区      ↑ Boss血条(出现时在顶部中央)
```

### 6.3 标题界面设计元素
```
居中：
  [LOGO] 星 际 雷 霆  (霓虹发光字，带扫光动画)
  [副标题] STELLAR THUNDER
  [装饰] 两侧战机剪影，背景星空+流星+扫描线

按钮（居中排列，发光边框）：
  ▶ 开始游戏
  🎮 选择模式
  ✈️ 战机仓库
  🏆 战绩排行
  ⚙️ 游戏设置
  
底部：
  [版本号 v1.0]   [最高分: 999999]
```

---

## 七、目录结构与代码架构

### 7.1 推荐目录结构（模块化拆分）
```
小游戏/
├── index.html              # 入口 HTML（加载所有脚本和样式）
├── css/
│   └── style.css           # 全局样式、UI 面板、动画
├── js/
│   ├── config/             # 📌 所有配置表（Builder 最常修改）
│   │   ├── gameConfig.js   # 游戏常量（画布大小、速度、概率）
│   │   ├── planeData.js    # 战机属性配置表
│   │   ├── enemyData.js    # 敌机配置表
│   │   ├── bossData.js     # Boss 配置+弹幕模式
│   │   ├── itemData.js     # 道具配置表
│   │   └── skillData.js    # 技能芯片配置表
│   │
│   ├── core/               # 🎮 游戏引擎核心
│   │   ├── Game.js         # 游戏主类（入口、主循环、状态机）
│   │   ├── Renderer.js     # 渲染器（Canvas 绘制工具函数）
│   │   ├── Input.js        # 输入管理（鼠标/触摸/键盘）
│   │   ├── Collision.js    # 碰撞检测（AABB+圆形判定）
│   │   └── Audio.js        # 音频管理器（Web Audio 合成）
│   │
│   ├── entities/           # 👾 游戏实体类
│   │   ├── Player.js       # 玩家战机类
│   │   ├── Enemy.js        # 敌机基类+子类
│   │   ├── Boss.js         # Boss 类（阶段切换AI）
│   │   ├── Bullet.js       # 子弹类（玩家弹/敌机弹）
│   │   ├── Item.js         # 道具类
│   │   ├── Particle.js     # 粒子类（爆炸/拖尾）
│   │   └── Wingman.js      # 僚机类
│   │
│   ├── systems/            # ⚙️ 子系统
│   │   ├── SpawnSystem.js  # 敌机/道具生成系统
│   │   ├── WaveSystem.js   # 波次/关卡管理系统
│   │   ├── SkillSystem.js  # 技能芯片效果管理
│   │   ├── UISystem.js     # HUD 和 UI 界面切换
│   │   └── SaveSystem.js   # localStorage 存档管理
│   │
│   └── utils/              # 🔧 工具函数
│       ├── Math2.js        # 数学工具（向量、角度、缓动）
│       ├── Pool.js         # 对象池（子弹/粒子复用）
│       └── Easing.js       # 缓动函数集合
│
└── assets/                 # (可选) 如果不用程序化绘制，放图片
    └── README.md           # 说明：本项目默认用 Canvas 绘制一切，无需素材
```

### 7.2 最简单文件版本（快速原型）
如果 Solo Builder 觉得多文件太复杂，可以合并为 **3 个文件**：
- `index.html` - HTML + CSS 内联
- `game.js` - 所有游戏逻辑
- `config.js` - 所有配置数据

### 7.3 核心主循环（伪代码，供参考实现）
```javascript
// Game.js - 主循环
class Game {
  constructor() {
    this.canvas = document.getElementById("game");
    this.ctx = this.canvas.getContext("2d");
    this.state = "MENU"; // MENU / PLAYING / PAUSED / BOSS / GAMEOVER / VICTORY
    this.entities = { player:null, enemies:[], bullets:[], items:[], particles:[] };
    this.lastTime = 0;
    this.frame = 0;
  }
  
  start() {
    requestAnimationFrame((t) => this.loop(t));
  }
  
  loop(timestamp) {
    const dt = timestamp - this.lastTime; // 毫秒
    this.lastTime = timestamp;
    this.frame++;
    
    if (this.state === "PLAYING" || this.state === "BOSS") {
      this.update(dt);
    }
    this.render();
    
    requestAnimationFrame((t) => this.loop(t));
  }
  
  update(dt) {
    // 1. 输入更新 → 玩家移动
    this.input.update(this.entities.player);
    // 2. 生成系统 → 敌机/道具生成
    this.spawnSystem.update(this.frame, this.waveSystem.currentWave);
    // 3. 移动更新 → 所有实体位置
    this.moveEntities(dt);
    // 4. 碰撞检测 → 子弹打敌人/敌人撞玩家/道具拾取
    this.collision.checkAll(this.entities);
    // 5. 战斗更新 → 射击、伤害、死亡
    this.combatUpdate(dt);
    // 6. 效果更新 → 护盾倒计时、技能持续时间
    this.skillSystem.update(dt);
    // 7. 粒子更新 → 爆炸/拖尾
    this.updateParticles(dt);
    // 8. 清理越界实体
    this.cleanupOutOfBounds();
    // 9. UI 同步（每 5 帧）
    if (this.frame % 5 === 0) this.ui.syncHUD();
  }
  
  render() {
    // 1. 清屏 + 背景星空
    this.renderer.drawBackground(this.frame);
    // 2. 绘制道具（底层）
    this.entities.items.forEach(i => this.renderer.drawItem(i));
    // 3. 绘制敌机
    this.entities.enemies.forEach(e => this.renderer.drawEnemy(e));
    // 4. 绘制 Boss
    if (this.entities.boss) this.renderer.drawBoss(this.entities.boss, this.frame);
    // 5. 绘制子弹（玩家弹在上，敌机弹在下）
    this.entities.enemyBullets.forEach(b => this.renderer.drawBullet(b, "enemy"));
    this.entities.bullets.forEach(b => this.renderer.drawBullet(b, "player"));
    // 6. 绘制玩家 + 护盾 + 尾焰
    this.renderer.drawPlayer(this.entities.player, this.frame);
    // 7. 绘制粒子
    this.entities.particles.forEach(p => this.renderer.drawParticle(p));
    // 8. Boss 血条 / 警告线等覆盖层
    if (this.state === "BOSS") this.renderer.drawBossUI(this.entities.boss);
  }
}
```

---

## 八、关键数据结构定义

### 8.1 通用实体基类（所有实体继承它）
```javascript
class Entity {
  constructor(x, y, width, height) {
    this.x = x;           // 中心 X 坐标
    this.y = y;           // 中心 Y 坐标
    this.vx = 0;          // X 方向速度
    this.vy = 0;          // Y 方向速度
    this.width = width;   // 碰撞盒宽
    this.height = height; // 碰撞盒高
    this.hp = 1;          // 生命值
    this.dead = false;    // 死亡标记（下一帧清理）
    this.type = "entity"; // 类型标签
  }
  
  // 矩形碰撞检测
  hit(other) {
    return Math.abs(this.x - other.x) < (this.width + other.width)/2
        && Math.abs(this.y - other.y) < (this.height + other.height)/2;
  }
  
  // 圆形碰撞检测（更精准的子弹判定）
  hitCircle(other, r1, r2) {
    const dx = this.x - other.x;
    const dy = this.y - other.y;
    return dx*dx + dy*dy < (r1 + r2) * (r1 + r2);
  }
}
```

### 8.2 玩家状态结构
```javascript
{
  // 基础
  x: 240, y: 700,
  hp: 100, maxHp: 100,
  speed: 5,
  planeType: "F01",
  
  // 战斗
  powerLevel: 1,          // 火力等级 1-8
  shieldLayers: 0,        // 护盾层数
  bombs: 3,               // 清屏炸弹数量
  skillEnergy: 100,       // 技能能量 0-100（满了可释放）
  skillCooldownTimer: 0,  // 技能冷却
  
  // 增益效果（临时）
  invincibleTimer: 0,     // 无敌时间（ms）
  berserkTimer: 0,        // 暴走状态时间
  magnetRange: 60,        // 道具磁吸范围
  
  // 技能芯片数组（每局叠加）
  equippedChips: [
    { id: "fire_core", level: 2, effect: "damage+20%" }
  ],
  
  // 统计
  killCount: 0,
  comboCount: 0,
  comboTimer: 0,          // 连击保持计时器
  score: 0
}
```

### 8.3 关卡波次配置示例
```javascript
const CHAPTER_1 = [
  // 第1关：3波小怪
  {
    waves: [
      { delay: 500, enemies: [{type:"scout", count:5, pattern:"line", gap:500}] },
      { delay: 3000, enemies: [{type:"scout", count:8, pattern:"v_formation"}] },
      { delay: 3000, enemies: [
          {type:"scout", count:6, pattern:"zigzag"},
          {type:"fighter", count:2, pattern:"random_top", delay: 1500}
      ]},
    ],
    reward: { coins: 100, exp: 50 }
  },
  // 第2关：加入轰炸机 + 小Boss
  {
    waves: [ /* ... */ ],
    hasSubBoss: true,
    subBossId: "SUBOSS_CH1",
    subBossWave: 3
  },
  // 第5关：章节Boss
  {
    waves: [ /* ...2波铺垫小怪... */ ],
    hasBoss: true,
    bossId: "BOSS_CH1"
  }
]
```

---

## 九、音效系统设计（Web Audio 程序化合成）

**零素材依赖**，全部用 Web Audio API 振荡器合成：

| 音效事件 | 合成方案 |
|---------|---------|
| **射击声** | 方波 800Hz → 400Hz 下滑，ADSR 0.01/0.05/0/0.02 |
| **子弹命中** | 三角波 200Hz，短促 |
| **敌机爆炸** | 白噪声 + 低频正弦爆破音 |
| **Boss 爆炸** | 多层噪声叠加 + 频率扫描 + 延迟混响 |
| **道具拾取** | 正弦波 523Hz → 659Hz → 784Hz 三连音（C-E-G） |
| **火力升级** | 上行音阶 C-D-E-F-G-A-B-C |
| **玩家中弹** | 锯齿波 150Hz 下扫 + 噪声 |
| **必杀释放** | 白噪声渐变 + 频率快速上升 |
| **Boss 警告** | 800Hz 方波，间隔 200ms 的 5 连短音 |
| **BGM** | 循环 16 小节合成鼓点 + 贝斯 + 琶音，Boss 战时节奏 ×1.5 |

> （如果 Builder 觉得合成音频太复杂，可降级为：找 8-10 个免费音效 mp3 放到 assets/sfx/，用 `<audio>` 标签播放即可。）

---

## 十、性能优化要点（保证 60FPS）

1. **对象池技术**：子弹/粒子高频创建销毁的对象，用 Pool 类复用，避免 GC 卡顿
2. **分层渲染**：背景星空 preRender 到离屏 canvas，每帧只偏移不重绘
3. **批量绘制**：同类子弹（相同颜色/大小）用一次 fillRect 批量绘制
4. **屏幕裁剪**：越界实体立即标记 dead，不参与渲染和计算
5. **碰撞粗筛**：先按网格分区，只有同区实体才做精确碰撞
6. **移动端降频**：检测到帧率 < 30 时，自动降低粒子数量上限
7. **节流 UI**：HUD 更新每 5 帧一次，不要每帧操作 DOM

---

## 十一、开发里程碑（Builder 执行顺序）

> ✅ 建议 Solo Builder **严格按此顺序开发**，每完成一个里程碑就能看到可玩的进展。

| 阶段 | 里程碑 | 预计工作量 | 可演示效果 |
|-----|-------|-----------|-----------|
| M1 | **搭建框架** | 2h | Canvas 画满 + 星空背景 + 主循环运行 |
| M2 | **玩家操控** | 2h | 鼠标/触摸拖拽战机移动，边界限制 |
| M3 | **基础射击** | 2h | 玩家自动射击，子弹向上飞，出界销毁 |
| M4 | **敌机出现** | 3h | 侦察机直线下降，被击中爆炸，得分系统 |
| M5 | **碰撞检测** | 2h | 子弹打敌机 + 敌机撞玩家，血量扣减 + 死亡 |
| M6 | **道具系统** | 2h | 8 种道具掉落 + 磁吸 + 火力升级 Lv1-4 |
| M7 | **关卡系统** | 3h | 波次配置 + 闯关模式 1-1 到 1-3 |
| M8 | **首个 Boss** | 4h | Boss 登场 + 血条 + 2 阶段 + 3 种弹幕 |
| M9 | **技能芯片** | 3h | 3 选 1 界面 + 红绿蓝芯片效果叠加 |
| M10 | **必杀技** | 2h | 炸弹清屏 + 能量系统 + 屏幕震动 |
| M11 | **僚机系统** | 2h | 火力 Lv6 召唤僚机跟随射击 |
| M12 | **更多模式** | 4h | 无尽模式 + 限时挑战模式 |
| M13 | **UI 美化** | 3h | 标题界面 + 设置 + 结算页 + 霓虹风格 |
| M14 | **音效合成** | 3h | Web Audio 合成全部 SFX + BGM |
| M15 | **更多 Boss** | 4h | 第 2-6 章 Boss + 终 Boss 合体形态 |
| M16 | **第 2-6 架战机** | 3h | 战机解锁 + 属性差异 + 选择界面 |
| M17 | **Bug 打磨** | 3h | 难度平衡、碰撞判定调优、性能测试 |
| **合计** | — | **~ 50 小时** | 完整可玩版本 |

---

## 十二、给 Solo Builder 的实现建议

### 12.1 推荐开发策略：渐进增强
```
先做 M1-M5（基础可玩） → 自测通过
    ↓
再做 M6-M8（核心乐趣：道具+关卡+Boss） → 这时候游戏已经好玩了！
    ↓
然后 M9-M12（进阶系统） → 内容丰富度↑
    ↓
最后 M13-M17（打磨） → 商业品质
```

### 12.2 配置优先，代码通用
- **先填配置表，再写通用代码**：比如先写 1 个通用 `Enemy` 类，然后通过 `enemyData.js` 配置 5 种敌机的参数，而不是写 5 个 Enemy 子类
- **弹幕做成参数化函数**：`createSpreadBullet(x, y, count, spreadAngle, speed, color)` 一个函数搞定所有扇形/环形弹幕

### 12.3 调试技巧
- 在 canvas 左上角加 **F1 调试面板**：显示帧率、实体数量、当前火力等级、Boss 血量百分比
- **F2 键**：直接跳到下一波 / 直接触发 Boss
- **F3 键**：+10 火力等级，方便测试 MAX 火力弹幕

---

## 附录 A：概率掉落算法（可直接用）
```javascript
function rollItemDrop() {
  const r = Math.random() * 100;
  if (r < 12) return "P";      // 12%
  if (r < 18) return "S";      // 6%
  if (r < 23) return "H";      // 5%
  if (r < 43) return "C";      // 20%
  if (r < 51) return "E";      // 8%
  if (r < 54) return "B";      // 3%
  if (r < 58) return "X";      // 4%
  if (r < 73) return "EXP";    // 15%
  return null;                 // 27% 不掉
}
```

## 附录 B：Canvas 绘制发光效果的通用函数
```javascript
function drawGlowRect(ctx, x, y, w, h, color, glowIntensity = 15) {
  ctx.save();
  ctx.shadowColor = color;
  ctx.shadowBlur = glowIntensity;
  ctx.fillStyle = color;
  ctx.fillRect(x - w/2, y - h/2, w, h);
  ctx.restore();
}

function drawGlowText(ctx, text, x, y, color, fontSize = 24, glow = 20) {
  ctx.save();
  ctx.font = `bold ${fontSize}px "Orbitron", monospace`;
  ctx.textAlign = "center";
  ctx.textBaseline = "middle";
  ctx.shadowColor = color;
  ctx.shadowBlur = glow;
  ctx.fillStyle = color;
  ctx.fillText(text, x, y);
  // 第二层叠色加深
  ctx.shadowBlur = 0;
  ctx.globalAlpha = 0.9;
  ctx.fillText(text, x, y);
  ctx.restore();
}
```

---

📌 **文档结束标志**：以上是《星际雷霆》飞机大战的完整技术设计文档，供 Solo Builder 理解和实现。请在优化后确认以下关键信息：
1. 是否需要增加/删除某些玩法系统？
2. 战机数量和 Boss 数量是否符合预期？
3. 视觉风格是否接受「赛博朋克霓虹科技风」？
4. 是否有特殊的品牌元素或彩蛋需要加入？
